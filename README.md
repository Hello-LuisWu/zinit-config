# zinit-config

## install zinit amd plugins

```sh
bash -c "$(curl --fail --show-error --silent --location https://raw.githubusercontent.com/Hello-LuisWu/zinit-config/refs/heads/main/install.sh)"
```

用 `exec zsh` 命令重启 zsh
